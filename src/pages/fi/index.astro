---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/ArticleCard.astro';
import TrendingTicker from '../../components/TrendingTicker.astro';
import UpcomingRaces from '../../components/UpcomingRaces.astro';
import { getCollection } from 'astro:content';
import { ui } from '../../i18n/ui';

const lang = 'fi';
const t = ui[lang];

const articles = (await getCollection('articles', ({ data }) =>
  data.language === 'fi' && !data.draft
)).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const slugFor = (a: any) => `/fi/aikakone/${a.slug.replace(/^fi\//, '')}`;
const imgFor = (a: any) => a.data.card_image || a.data.featured_image;

// Featured grid: 1 big + 2 stacked
const bigCard = articles[0];
const stackedCards = articles.slice(1, 3);

// Secondary row: 4 cards
const secondaryCards = articles.slice(3, 7);

// Ticker: latest 5 article titles
const tickerItems = articles.slice(0, 5).map(a => ({
  href: slugFor(a),
  title: a.data.title,
}));

const galleries = await getCollection('galleries');
const featuredGalleries = galleries.slice(0, 4);

// Podcast episodes (MVP: placeholder image for all)
const podcastEpisodes = [
  {
    title: 'Heikki Mikkola: Nelinkertainen Motocrossin Maailmanmestari',
    subtitle: 'Tarina legendaarisesta suomalaiskuljettajasta',
    href: '/fi/podcast#ep-0',
  },
  {
    title: 'Honda CR250: Legendan Synty ja Loppu',
    subtitle: 'Motocrossin ikonisimman pyörän tarina',
    href: '/fi/podcast#ep-1',
  },
  {
    title: 'Suzuki Motocross Racing: From Disastrous Start to Golden Era',
    subtitle: 'Suzukin tie motocrossin kultaiselle aikakaudelle',
    href: '/fi/podcast#ep-2',
  },
  {
    title: 'Motocross of Nations: Historia ja Voittajat',
    subtitle: 'Motocross of Nations -kilpailun legendat',
    href: '/fi/podcast#ep-3',
  },
];
---

<BaseLayout
  title="Etusivu"
  lang={lang}
  description="Motocrossin ja moottoripyöräurheilun kuvia, tarinoita ja tuloksia 1960-luvulta nykypäivään. MXGP 2026, Aikakone-arkisto ja valokuvagalleria."
>
  <TrendingTicker label="Nyt luetuimmat" items={tickerItems} slot="pre-header" />

  {/* ===== Typographic hero band ===== */}
  <section class="hero-band">
    <div class="container">
      <h1 class="hero-band__title">Vauhtia ja elämää linssin läpi</h1>
      <p class="hero-band__subtitle">Every frame tells a story</p>
    </div>
  </section>

  {/* ===== Featured grid (articles) ===== */}
  {bigCard && (
    <section class="section featured-grid-section">
      <div class="container">
        <div class="section-header-row">
          <h2 class="section-heading">Uusimmat artikkelit</h2>
          <a href="/fi/aikakone" class="section-all-link">Kaikki artikkelit →</a>
        </div>
        <div class="hero-split">
          <div class="hero-split__big">
            <ArticleCard
              href={slugFor(bigCard)}
              image={imgFor(bigCard)}
              category={bigCard.data.category}
              title={bigCard.data.title}
              subtitle={bigCard.data.subtitle}
              size="lg"
            />
          </div>
          <div class="hero-split__stack">
            {stackedCards.map(a => (
              <ArticleCard
                href={slugFor(a)}
                image={imgFor(a)}
                category={a.data.category}
                title={a.data.title}
                size="md"
              />
            ))}
          </div>
        </div>

        {secondaryCards.length > 0 && (
          <div class="secondary-row">
            {secondaryCards.map(a => (
              <ArticleCard
                href={slugFor(a)}
                image={imgFor(a)}
                category={a.data.category}
                title={a.data.title}
                size="sm"
              />
            ))}
          </div>
        )}
      </div>
    </section>
  )}

  {/* ===== Upcoming races band ===== */}
  <UpcomingRaces />

  {/* ===== Gallery section (1 big + 3 stacked) ===== */}
  {featuredGalleries.length > 0 && (
    <section class="section gallery-section">
      <div class="container">
        <div class="section-header-row">
          <h2 class="section-heading">Galleria</h2>
          <a href="/fi/galleria" class="section-all-link">Kaikki galleriat →</a>
        </div>

        <div class="hero-split">
          <div class="hero-split__big">
            <ArticleCard
              href={`/fi/galleria/${featuredGalleries[0].data.slug}`}
              image={`/galleries/${featuredGalleries[0].data.slug}/${featuredGalleries[0].data.cover_image}`}
              title={featuredGalleries[0].data.title}
              subtitle={t['gallery.photoCount'].replace('{count}', featuredGalleries[0].data.images.length.toString())}
              size="lg"
            />
          </div>
          <div class="hero-split__stack">
            {featuredGalleries.slice(1, 4).map(gallery => (
              <ArticleCard
                href={`/fi/galleria/${gallery.data.slug}`}
                image={`/galleries/${gallery.data.slug}/${gallery.data.cover_image}`}
                title={gallery.data.title}
                subtitle={t['gallery.photoCount'].replace('{count}', gallery.data.images.length.toString())}
                size="md"
              />
            ))}
          </div>
        </div>
      </div>
    </section>
  )}

  {/* ===== Podcast section (1 big + 3 stacked) ===== */}
  <section class="section podcast-section">
    <div class="container">
      <div class="section-header-row">
        <h2 class="section-heading">Podcast</h2>
        <a href="/fi/podcast" class="section-all-link">Kaikki jaksot →</a>
      </div>

      <div class="hero-split">
        <div class="hero-split__big">
          <ArticleCard
            href={podcastEpisodes[0].href}
            image="/images/hero-podcast.jpg"
            title={podcastEpisodes[0].title}
            subtitle={podcastEpisodes[0].subtitle}
            size="lg"
          />
        </div>
        <div class="hero-split__stack">
          {podcastEpisodes.slice(1, 4).map(ep => (
            <ArticleCard
              href={ep.href}
              image="/images/hero-podcast.jpg"
              title={ep.title}
              subtitle={ep.subtitle}
              size="md"
            />
          ))}
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* ===== Typographic hero band ===== */
  .hero-band {
    background: #000;
    padding: var(--space-5) 0 var(--space-4);
    border-bottom: 1px solid rgba(255, 153, 0, 0.15);
    text-align: center;
  }

  .hero-band__title {
    font-family: var(--font-headline);
    font-size: clamp(1.8rem, 4.5vw, 2.8rem);
    font-weight: 900;
    color: var(--brand-primary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    line-height: 1.1;
    margin: 0 0 var(--space-2);
  }

  .hero-band__subtitle {
    font-family: var(--font-headline);
    font-size: clamp(0.9rem, 1.3vw, 1.1rem);
    font-weight: 600;
    color: rgba(255, 255, 255, 0.55);
    letter-spacing: 0.5px;
    margin: 0;
  }

  /* Section shared */
  .section-header-row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: var(--space-4);
    gap: var(--space-3);
  }

  .section-heading {
    font-family: var(--font-headline);
    font-size: 1.75rem;
    font-weight: 900;
    color: var(--text-primary);
    margin: 0;
  }

  .section-all-link {
    font-family: var(--font-headline);
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--brand-primary);
    text-decoration: none;
    white-space: nowrap;
  }
  .section-all-link:hover {
    text-decoration: underline;
  }

  /* ===== Shared: 1 big + N stacked layout (hero-split) ===== */
  .hero-split {
    display: grid;
    grid-template-columns: 1.6fr 1fr;
    gap: var(--space-3);
    align-items: stretch;
  }

  .hero-split__big {
    display: flex;
    min-height: 500px;
  }
  .hero-split__big > article {
    width: 100%;
  }

  .hero-split__stack {
    display: grid;
    grid-template-rows: repeat(auto-fit, minmax(0, 1fr));
    gap: var(--space-3);
  }
  .hero-split__stack > article {
    min-height: 0;
  }

  /* Articles section */
  .featured-grid-section {
    background: var(--bg-secondary);
    padding-top: var(--space-5);
    padding-bottom: var(--space-6);
  }

  /* Secondary row: 4 cards */
  .secondary-row {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-3);
    margin-top: var(--space-3);
  }

  /* Gallery section */
  .gallery-section {
    background: white;
    padding: var(--space-6) 0;
  }

  /* Podcast section */
  .podcast-section {
    background: var(--bg-secondary);
    padding: var(--space-6) 0;
  }

  /* Responsive breakpoints */
  @media (max-width: 900px) {
    .hero-split {
      grid-template-columns: 1fr;
    }
    .hero-split__big {
      min-height: 320px;
    }
    .hero-split__stack {
      grid-template-rows: auto;
      grid-template-columns: repeat(3, 1fr);
    }
    .secondary-row {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .hero-split__stack {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 540px) {
    .hero-split__stack {
      grid-template-columns: 1fr;
    }
    .secondary-row {
      grid-template-columns: 1fr;
    }
    .section-heading {
      font-size: 1.4rem;
    }
  }
</style>
